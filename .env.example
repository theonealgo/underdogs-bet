# Copy this to .env and fill in your values
# The app works without these — they're optional overrides

# Stripe donation payment link
STRIPE_DONATION_URL=https://buy.stripe.com/your_link_here

# Port (defaults to 5000 locally, auto-set by host in production)
PORT=5000
