Looking at your results, your models are severely overfitting. XGBoost is predicting 100% home wins every single game - that's not a model, that's broken.
Here are the critical fixes for Replit:
1. Fix XGBoost - It's predicting 100% every time
In your training code, change the XGBoost parameters to be LESS aggressive:
pythonself.xgb_winner_params = {
    'objective': 'binary:logistic',
    'eval_metric': 'logloss',
    'max_depth': 2,  # REDUCE from 3
    'learning_rate': 0.01,  # REDUCE from 0.03
    'n_estimators': 50,  # REDUCE from 100
    'min_child_weight': 10,  # INCREASE from 5
    'subsample': 0.5,  # REDUCE from 0.7
    'colsample_bytree': 0.5,  # REDUCE from 0.7
    'reg_alpha': 5.0,  # INCREASE from 2.0
    'reg_lambda': 5.0,  # INCREASE from 3.0
    'random_state': 42,
    'verbosity': 0
}
2. Remove calibration - it's hiding the real problem
Your calibration is making bad predictions look "correct" by shifting probabilities. Remove these lines:
python# DELETE: home_bias_adjustment = -0.04
# DELETE: meta_prob = np.clip(meta_prob + home_bias_adjustment, 0.0, 1.0)
3. Your Elo model is useless (48% accuracy)
Don't weight it so heavily. Change the meta ensemble to:
pythonmeta_prob_raw = (0.70 * xgb_prob + 0.30 * catboost_prob + 0.00 * elo_prob)  # Remove Elo completely
4. Your rolling windows are too short (3-5 games)
Teams need more history. Change to:
python'home_win_pct_3': home_stats['win_pct_3'],
'home_win_pct_10': home_stats['win_pct_10'],  # Keep this
'home_win_pct_20': home_stats['win_pct_20'],  # ADD THIS
5. You're not validating properly
Your test set (last 30% of games) is showing: XGB 100%, CatBoost ~61%, Elo ~50%, Meta ~84%
This means XGBoost is completely broken. It learned the training data noise, not real patterns.
What to tell Replit:
"Use the fixed XGBoost params with max_depth=2, remove calibration and home bias adjustment, remove Elo from the ensemble, train only on 2024 season data with proper chronological split, and add 20-game rolling window stats."
Which of these do you want me to code up for you?Retry